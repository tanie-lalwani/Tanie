# Frontend env used by the existing contact form.
VITE_FORMSPREE_ENDPOINT=https://formspree.io/f/mpqypqka

# Server-only env. Keep this out of the browser bundle.
# Copy this file to .env.local and fill in your server-side secrets.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
